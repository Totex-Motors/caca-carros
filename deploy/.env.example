# Copie para deploy/.env (o install.sh faz isso por voce).

# Subdominio onde o app vai rodar
DOMAIN=carros.grupocardoso.online

# Login do app (criado na primeira vez que o servidor sobe)
ADMIN_EMAIL=
ADMIN_PASSWORD=

# Gerados automaticamente pelo install.sh
POSTGRES_PASSWORD=
JWT_SECRET=
JWT_EXPIRES_IN=8h

# IA (dossie tecnico e analise de anuncios). Configure com: bash deploy/set-openai-key.sh
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.5
OPENAI_REASONING_EFFORT=medium

# Buscas
EXTERNAL_SEARCH_ENABLED=true
# Rodada automatica: OFFICIAL = a cada 12h (00:00 e 12:00); TEST = a cada 11 min
CAR_SEARCH_MODE=OFFICIAL
CAR_CLEANUP_CRON=0 3 * * *
CAR_RETENTION_DAYS=30

# Busca nos portais pelo Actor proprio da Apify (Webmotors + OLX + Mercado Livre numa execucao so).
# Grave com: bash deploy/set-apify-key.sh  (nao cole o token aqui a mao nem no chat)
APIFY_TOKEN=
APIFY_SCRAPER_ACTOR_ID=
# Opcionais: limite de anuncios e paginas por portal, memoria da execucao (MB)
APIFY_SCRAPER_MAX_RESULTS=30
APIFY_SCRAPER_MAX_PAGES=2
APIFY_SCRAPER_MEMORY_MB=2048

# Caminho antigo, usado so quando APIFY_SCRAPER_ACTOR_ID estiver vazio
APIFY_ACTOR_ID=ribtools/webmotors-scraper
APIFY_TIMEOUT_MS=180000

# Proxy da OLX/Mercado Livre no caminho antigo (opcional)
PROXY_HOST=br.decodo.com
PROXY_PORT=
PROXY_PORTS=
PROXY_USER=
PROXY_PASS=
OLX_ADS_LIMIT=10
OLX_MAX_PAGES=1
OLX_RETRY_ATTEMPTS=2
OLX_NAV_TIMEOUT_MS=90000
OLX_SEARCH_TIMEOUT_MS=180000
